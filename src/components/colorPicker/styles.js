import { motion } from 'framer-motion';
import styled from 'styled-components';

const StyledAnimationWrapper = styled(motion.div)`
  position: relative;
  z-index: 10;
`;

export { StyledAnimationWrapper };
